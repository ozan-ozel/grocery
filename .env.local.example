# Loaded by scripts/upload-nutrition.ts via `node --env-file=.env.local`.
# Copy to .env.local. Uses the SERVICE ROLE key (bypasses RLS) — never ship it.
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
USDA_API_KEY=your-usda-api-key
