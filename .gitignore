node_modules
dist
*.tsbuildinfo
.dev.vars
.env
.env.local
.env.*.local
.wrangler

# Local Netlify folder
.netlify
deno.lock

# Claude Code worktrees
.claude/worktrees

# Subagent-driven-development scratch workspace (ledgers, task briefs/reports)
.superpowers

# Serena (optional MCP server). project.yml is checked in; everything else is
# per-developer state (cached indexes, personal memories, local overrides).
.serena/cache/
.serena/memories/
.serena/project.local.yml
.vercel
.env*

# ---------------------------------------------------------------------------
# Nutrition Curriculum project (nutrition-curriculum/)
# ---------------------------------------------------------------------------
# Original source textbooks (PDF/EPUB) -- copyrighted commercial books, not
# this project's own output. Kept on disk for reference, never versioned.
nutrition-curriculum/01_SOURCE_BOOKS/

# Common temporary/system files
*.tmp
*.temp
*.log
.DS_Store
Thumbs.db
